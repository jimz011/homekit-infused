# These are the decluttering templates. This file will
# only redirect to the corresponding templates. You will
# need this file or templates will not work!

# Mini Graph Card
mini-graph:
  !include mini-graph-template.yaml

# Laundry Picture Elements
laundry:
  !include laundry-template.yaml

# Person Picture Elements
person:
  !include person-template.yaml

# Climate Thermostat Card
climate:
  !include climate-template.yaml

# Light Popup Card
more-info-popup:
  !include more-info-popup-template.yaml 

# Button Card (This is the main template used everywhere)
button:
  !include button-template.yaml

# Header Text
header:
  !include header-template.yaml

# Check Button Card
check-button:
  !include check-button-template.yaml

# Notification Card (Frontend)
notification: 
  !include notification-template.yaml

# Quick Access Menu
quick-access-menu:
  !include quick-access-menu-template.yaml

# Person Detailed Info Template
person-more-info:
  !include person-more-info-template.yaml

# Birthday Frontpage Notifications Popup
birthday:
  !include birthday-notification-template.yaml

# Energy Usage Popup
energy-usage:
  !include energy-usage-popup-template.yaml