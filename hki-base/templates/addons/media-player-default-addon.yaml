# lovelace_gen

type: media-control
style: |
  ha-card {
    border-radius: var(--border-radius);
    box-shadow: var(--box-shadow);
  }
  .player {
    background: var(--ha-card-background) !important;
  }
entity: {{ entity }}
name: {{ name }}