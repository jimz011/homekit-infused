# lovelace_gen

# card
type: markdown
style: |
  ha-card {
    box-shadow: none;
    height: 33px;
    background: rgba(0,0,0,0.0);
    padding: 0px 11px;
    margin-left: 10px
    color: var(--category-text);
  }
content: "{{ content }}"
